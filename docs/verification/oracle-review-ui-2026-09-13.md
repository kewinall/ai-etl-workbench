# 歷史標準答案唯讀回讀

## 已部署

每個答案版本提供「查看答案內容」。後端先以 Task / Run / Specification / Oracle ID 查核歸屬，再解密已保存文件；回應不含密文、nonce 或金鑰。資料列每頁 50 筆，API 上限 100 筆；回傳總筆數與下一頁狀態。數值轉顯示字串保留 BIGINT / Decimal 精度，NULL 和空字串分別顯示。

成功回應 Cache-Control=no-store、Pragma=no-cache。網站不使用 localStorage 或 sessionStorage 保存答案，關閉時清除該元件顯示資料。這不表示能抹除使用者瀏覽器開發工具或作業系統記憶體中的資料。

歷史規格失效後仍可查閱，但此操作不恢復核准有效性、不產生 QA 或 Release 通過。原始版本維持不可變。

## 證據

- 隔離 PostgreSQL / API 整合：5 passed，1.26 秒。舊規格回讀、跨 Task 拒絕、內容 checksum、分頁空結果及 API 上限。
- 本機分頁／精度／解密／API 回歸：17 passed，0.36 秒。
- 前端建置通過；API、web、CONTROL 已重建部署。
- 部署後瀏覽器：3 passed，7.8 秒。真實答案保存 301.35、改版 302.35，重新載入後分別回讀兩版本；關閉移除顯示、390px 無溢出。另含合成歷史與原六頁籤／節點／下載契約。
- 真實案例 Task TASK-20260913-0192，Run 0a72e18f-3b0a-46bd-af40-b3b43c58433f，規格 e4cd71bb-858a-4112-a329-72f6bb79d2de。

## 限制

分頁精度有單元測試，但多頁切換尚未做真實瀏覽器大量資料驗收。歷史回讀目前唯讀，不直接載入編輯器；答案核准仍限本次表格保存並核對的版本。大型答案匯入、型別數值精度／長度限制、真實 Vertica QA、SDM、Release 與四情境／20 案例仍未完成。未新增模型請求或 Vertica 寫入。
