# 標準答案欄位表單與人工核准

## 已部署內容

Task「執行與 QA」依已核准且目前有效規格開放答案表單。欄位順序與型別由後端編譯結果提供；支援資料列新增／刪除、欄位 NULL 政策、逐格 NULL、文字／整數／小數／布林輸入。

整數以 BigInt 檢查 signed 64-bit 範圍並序列化為精確 JSON 整數；Decimal 保留十進位字串。禁止溢位、不明布林與小數科學記號。零筆答案代表預期沒有任何輸出，不授予 QA 通過。

保存後核對本機文件 SHA-256、API 回傳與歷史最新版本，再開放本次表格的人工核准。核准綁定 oracle ID 與 checksum，核准後再次回讀。修改表格清除核准選擇，須重新保存。失敗保留表格；不自動重試。Run／規格切換與全域導覽使用草稿保護，操作處理中阻擋離開。

## 實測

- 正式前端建置通過，API／web／CONTROL 重建部署；未啟用 ETL 或模型派發。
- 真實 UI → API → 隔離 PostgreSQL：建立規格、核准規格、答案保存、答案核准、答案改版不繼承核准、頁面重新載入及歷史查阅通過。
- 最新案例 Task `TASK-20260913-0188`；Run `8d60347b-6494-4b3a-9d00-6c6acd02ff55`；規格 `d0075a75-3244-4bf7-a9df-38f820eed6d7`。
- 保存失敗為瀏覽器注入 HTTP 503，驗證保留草稿及不提供核准；其他保存／核准呼叫是真實 API 與 PostgreSQL。
- 完整網站套件 25 passed、2 skipped，50.8 秒。包含兩項純答案序列化測試，不能將全部 25 項稱為真實整合；跳過付費 Copilot 與 opt-in native Worker lifecycle。
- 安全後端回歸 363 passed、73 skipped，5.47 秒；排除舊 `test_api.py` 主機資料庫測試，未執行外部 DB／模型。
- 前一輪答案欄位 API 整合 15 passed，1.36 秒；型別回歸 16 passed，0.80 秒。

測試恢復原平台連線設定，取消自身非寫入 Run，保留測試歷史。未變更原 Windows DB、業務表或歷史檔案。

## 仍待完成

歷史答案不提供解密回讀，因此目前只能核准本次表格保存且確認的版本；重新整理後須重新輸入／保存，不能盲核准不可見資料。大型答案匯入及 NUMERIC precision/scale、VARCHAR 值長度對應 DDL 的完整限制仍待補齊。尚未連接真實 Vertica 結果查詢、持久 QA 證據、SDM、Release，也未完成 P0–P3 四案例及 20 案例完整驗收。本次沒有追加 Copilot 呼叫或 Vertica 寫入。
