# 第 3 階段：多來源 Join（未完成）

日期：2026-09-26。前置：第 2 階段日期案例已正式交付；不得重跑其目標寫入。

## 本次修改

- `target_config.join_contract_v1`：獨立版本契約，初期涵蓋兩來源、單一 INNER／LEFT 等值 Join。
- 明確提供左右來源、鍵值、null 不相符、重複鍵展開、區分大小寫且不 trim；不填就不推測。
- 每側鍵值必須在該側來源唯一存在，不能利用另一側同名欄位補足，也不能重複指定鍵。
- 既有 Run revision API 新增 typed `join_contract_v1`，新 checksum 與重新核准，保持原 Task 系統。
- Public Run／SA evidence 只呈現通過 schema 的語意欄位；無效結構不回傳原始內容。
- 單來源未提供 Join 的既有序列化內容不加預設欄位；既有 Release 不改寫。

## 已執行驗證

1. 本機 targeted tests：55 passed（0.48s）。
2. `ai-etl-locked-regression`，repository 原始碼唯讀掛載，既有鎖定依賴測試映像：
   894 passed、25 skipped、1 warning（21.06s），exit 0。
3. 新 PostgreSQL integration test 實際驗證：
   - 缺 Join 條件的 parent 被阻擋。
   - 補正產生 child、新 input checksum，不複製核准。
   - 同 request key 相同內容取得同 child；更換 LEFT→INNER 則衝突。
   - 未核准 child 不領取；核准後 Gate 重跑。
   - parent snapshot／Gate evidence 不變，沒有寫入開始。
4. 測試 DB 不連接真實 Vertica，不呼叫模型；容器正常停止，測試 volume 保留。
5. 25 個 skip 是原生／指定現有證據／Git distribution 前置條件，不能當成通過。

## 尚未完成與下一個接點

目前仍刻意阻擋多來源執行，不能把「有 Join 契約」說成「已完成 Join」。
本次沒有新增網站 Join 表單、沒有啟用多来源模型派送、沒有新模型或 Vertica 寫入。

接續順序：

1. 版本化多來源 CSV 輸入契約：每個 source_ref 綁定獨立格式／upload checksum／staged file。
   不共用單一 source.csv 或偷偷以 source.0 代替其他來源。
2. 新規格版本與 source-qualified Naming Contract，精確比對已確認 Join 類型、鍵及 null/duplicate 政策。
   注入 INNER／LEFT 不一致時回報 specification field、Join node id、expected／actual；不可只回 generic unsupported。
3. 確認原生 Hop Join metadata／排序／null 與 duplicate 行為後，產生兩輸入及 Join 的 DAG。
4. 接通模型 schema／版本化 prompt、staging、QA、SDM、manifest、HWF 與 isolated replay。
5. 真實兩 CSV／Hop／Vertica標準答案比對，含 unmatched／null／duplicate keys；負向案例證明執行前攔截。
6. 完成上述驗證後才解除相應 Gate blocker；第 3 階段才可標示通過。

## 權限與範圍

使用者已授權合成 Pilot 自動核准；核准仍綁定版本與 checksum，不繞過 validator。
既有正式 Release 與歷史失敗保留。人工基準仍須真人實測，不能將代理操作時間充作人工時間。

## 每來源 CSV 契約與暫存驗證（後續進度）

新增 `source_config.csv_input_contracts_v1`，每個 `source.N` 綁定獨立的
`CsvInputContractV1`。必須精確涵蓋所有來源，拒絕缺漏、額外來源與單／多來源契約混用。
既有單 CSV 的 schema／context 不改成多來源格式。新 API revision 欄位採顯式轉換，
移除舊的單來源契約並產生新 checksum，改變任一來源格式不能重用原請求內容。

- Worker preflight 已依 source_ref 讀取對應契約，逐檔驗證上傳 bytes，失敗指出來源 index。
- `stage_csv_sources` 在所有檔案完成驗證前不 yield，並保留各自的 checksum、contract checksum、
  獨立 attempt 目錄。第二來源失敗或 caller 例外時清理本次副本，不刪除原始上傳檔案。
- 實際檔案測試使用兩個已保存的合成 CSV（逗號／pipe 分隔）；證明不能共用第一份格式，
  原始上傳變動不改變已暫存 bytes，遭竄改時不產生部分可執行输入。
- API／SA evidence 僅回傳白名單格式與來源參照，不含檔案路徑、samples 或連線機密。

驗證：

- 本機首次：48 passed、8 setup errors，原因是 Windows pytest 預設暫存目錄權限。
  未改弱測試；改用工作區內全新獨立暫存目錄後，最終 targeted 57 passed（0.58s）。
- 隔離 PostgreSQL 最終回歸：915 passed、25 skipped、1 warning（21.11s），exit 0。
  Run revision、契約轉換、重複請求內容衝突與重新核准均實際測試；測試容器正常停止。
- 沒有模型呼叫、真實 Vertica 寫入或 UI 功能驗收；未重建線上 API／Worker。

**剩餘接點**：多來源 staging helper 尚未接入正式 `execution_preparation`／`hop_command`／
`prepared_integrity`／`release_replay_worker`；這些仍為單 CSV，且 Gate 保留 Join runtime blocker。
下一步實作 V2 specification 與來源限定命名、精確 Join 語意錯誤及原生 Hop DAG，再一併接通
執行參數／授權 binding／QA／交付。不可僅移除 Gate 阻擋就視為支援 Join。

## V2 規格與精確 Join 語意攔截（程式層）

新增獨立 `EtlSpecificationV2`，不改寫 V1 schema。V2 明確要求兩個不同且順序固定的
source_refs，以及完整 Join 契約。現有 `validate_specification`／`compile_hpl` 入口已接上
語意檢查，INNER／LEFT、鍵值或節點 ID 與已確認輸入不符時，回傳：

- `SPEC_JOIN_SEMANTICS_MISMATCH`
- `field_path`（例如 `joins.0.join_type`）
- `requirement_path`（例如 `join_contract_v1.joins.0.join_type`）
- `node_id`、`proposed_node_id`
- `expected`、`actual`（僅 schema-validated 語意欄位）

測試從真實 compiler 入口注入 LEFT→INNER，確認沒有 HPL、plan 或可保存的規格 checksum；
既有 store 不會寫入此 INVALID 規格。另覆蓋缺契約、錯鍵、節點不同、stale input checksum、
重複／缺漏來源、未知 SQL 欄位與 strict version。原始输入不被修改。

本機 targeted 81 passed（0.36s）；隔離回歸 928 passed、25 skipped、1 warning（21.13s），exit 0。
沒有模型呼叫、Hop 執行或 Vertica 寫入。

**此部分仍不是第 3 階段驗收**：V2 即使 Join 語意相符仍回傳
`SPEC_JOIN_COMPILATION_NOT_READY`，不能保存核准。完整命名／型別驗證、正向 DAG 編譯、
原生 Hop null/duplicate 行為與執行／交付接點尚待實作；必須全部接通後才移除此暫時阻擋。

## 原生 Hop MergeJoin 證據

新增 `InspectJoinPlugin.java` 與 `ExecuteJoinProbe.java`，透過現有 Hop 2.12.0
plugin registry 取得真實 metadata，並由 LocalPipelineEngine 執行固定合成測試管線。
容器 `--network none`，僅允許 CSVInput／SortRows／MergeJoin／FilterRows／Dummy；
沒有資料庫、檔案輸出或任意程式節點。來源與 probe 唯讀掛載。

映像：`ai-etl-workbench-worker:dev`，本次 image ID
`sha256:665eb2e14db192580738fe0eb6168b82245ae56c43ddb2f5173b407c5fc6103c`。

已驗證八組（LEFT／INNER × 是否排除右側 null × 右側一般資料／全部 null），
加上發佈檔案檢查共 **9 passed（33.89s）**。逐筆比對保留重複列，不以筆數相同代替值相同：

- 相同鍵左 2 列、右 2 列 → 4 列，未自動去重。
- 區分 `A`／`a`／`A `，沒有 case folding 或 trim。
- 原生 MergeJoin 的 null 會匹配 null：左右 null payload 同時出現在輸出。
- 先以 FilterRows 排除右側 null 鍵，才符合 `NEVER_MATCH`。
- LEFT 保留無匹配的左列，右欄位為 null；INNER 不保留。
- 右側全部 null 被 Filter 排除、Join 接收到空資料流時，LEFT 仍保留全部左列，INNER 為 0 列。
- 檢查輸出 metadata 的四個欄位名称／順序與所有實際值。

首次四組測試因收集器誤把 row buffer 長度當成 metadata 欄數而失敗。
診斷明確取得 metadata=4、buffer=12 後，改為依 metadata 讀值，仍強制檢查欄位順序；
不是忽略 ETL 錯誤或修改標準答案。修正後先 5 passed，再擴充邊界至上述 9 passed。

此為**原生引擎語意證據，不是平台 Compiler 或 Vertica E2E 驗收**。
probe 管線由測試建構，尚非 V2 compiler 產物；單鍵字串已驗證，複合鍵／其他鍵型別仍需涵蓋。
編譯下一步必須產生「右側 null-key Filter → 各側 SortRows → MergeJoin」並綁定來源限定命名，
不能直接只產生 MergeJoin 就宣稱 `NEVER_MATCH` 正確。

重跑（Windows，使用全新 basetemp，禁止選取既有資料目錄）：

```powershell
$env:WORKBENCH_NATIVE_JOIN_TEST='1'
..\.venv\Scripts\python.exe -m pytest tests/test_join_native.py tests/test_source_distribution.py -q -p no:cacheprovider --basetemp <NEW_EMPTY_TEST_DIRECTORY>
```

執行位置為 `backend`。需 RockyLinux9、現有已建置 Worker 映像；不使用模型憑證或 Vertica。

最終控制平面回歸：928 passed、33 skipped、1 warning（20.78s），exit 0；
測試容器正常停止。新增八個 native 測試預設 opt-in，因此此處 skip；已由上方真實 Hop 執行證據補足，
但其他既有 skip 未在本次重驗。正式平台 V2 編譯／模型／Vertica／交付仍未驗收。

## V2 正向編譯已接通（最新判定）

本節取代前述「V2 尚不能編譯」的當時狀態，但不取代尚未完成的端到端驗收限制。

- V2 與 V1 共用輸入核准、run/settings checksum、目標、命名版本、型別／精度、Filter、
  aggregation、projection 驗證；V2 額外精確比對 Join intent。
- 命名來源使用 `source.0.<原欄名>`／`source.1.<原欄名>`，各側同名欄位不得混用。
- Join 鍵必須有命名對應、兩側型別 family 一致；不隱含轉型。Join node ID 不可佔用程式節點。
- 缺少欄名 metadata 不會靜默忽略；額外來源、舊命名、未核准輸入、不符目標與輸出均拒絕。
- 編譯 `SOURCE_CSV_0`／`SOURCE_CSV_1`，右側各鍵 `IS NOT NULL`，左右 SortRows，
  MergeJoin，再串接既有 post-Join Filter／aggregation／projection／TableOutput。
- Requirement Gate 只驗證需求完整性，不再以未實作編譯作為需求問題。
  **多來源 runtime 尚未接通，因此 `execution_authorization.offer` 明確回傳
  `MULTI_SOURCE_EXECUTION_NOT_READY`，在發出執行核准、排隊及 DDL 前阻擋。**

測試更新不是刪除防護：原「所有 Join 皆 unsupported」斷言改為測需求完整可 CHECKED，
另直接測 runtime authorization 拒絕多來源，且所有版本與 semantic mismatch 防護保留。

原生測試改為直接使用正式 `compile_hpl` 產物：只綁定 CSV fixture 路徑、把資料庫終點換成
Dummy 收集器，保留 compiler 的 CSVInput／FilterRows／SortRows／MergeJoin／SelectValues。
四組 compiler cases（LEFT／INNER × 右侧一般／全 null）逐筆通過；另四組手工管線保留為
原生 null 匹配對照。加上發佈檢查 **9 passed（34.64s）**。

Targeted **110 passed（0.42s）**；最終隔離 PostgreSQL 回歸
**938 passed、33 skipped、1 warning（20.71s）**，exit 0，測試服務正常停止。
無模型呼叫、Vertica 寫入或線上服務重建；第 3 階段仍未整體驗收。

下一步：接通 multi-source execution binding／prepared integrity／Hop command，再整合
Developer V2 schema 與 prompt、QA、SDM、manifest/HWF 及 isolated replay；最後做真實同 Run 案例。
複合鍵與其他鍵型別的原生案例尚需擴充，不應將單鍵字串結果泛化成全部型別已驗證。

## 多來源執行準備與原生 CLI 接通（尚未授權正式派送）

- 多來源以同一 attempt 根目錄下的 `source-0/source.csv`、`source-1/source.csv` 保存，
  每份仍各自驗證來源 bytes／CSV 契約，失敗清理整個 attempt，不修改原始上傳。
- 新增 path-free `source_checksums` 與由完整 source_ref→checksum map 計算的 `source_checksum`。
  交換來源順序、少一份或更改任何一份都會改變整組 binding。V1 保留原欄位格式。
- `execution_preparation` 支援 V2，staging 後重新載入核准、規格與 HPL checksum；變動就拒絕。
- `prepared_integrity` 檢查兩份檔案內容、固定位置、完整 binding 及 symlink/reparse-point 祖先；
  不接受單／多來源混用。這不是防止本機管理員在最後檢查後修改檔案的絕對保證。
- `hop_command` 只允許明確的 1／2 來源，產生固定 `SOURCE_CSV_0`／`SOURCE_CSV_1` 參數；
  不開放任意參數、程式或路徑欄位。`hop_cli` 使用已通過完整性驗證的來源數量。

實際測試：

- Targeted 檔案／binding／CLI 測試 **70 passed（0.57s）**，包含兩側竄改、刪除、換路徑、
  錯誤 checksum、缺少 binding、核准改變及清理。
- Worker image 掛載本次 backend 原始碼唯讀，`--network none`：
  `test_multi_hop_cli_native.py` **2 passed（6.91s）**。
- 使用正式 V2 compile_hpl、兩份不同分隔符號 CSV、實際 source staging／integrity／hop-run CLI；
  確認 source_0 輸出 3 列、source_1 輸出 2 列，右側 null 排除 1 列，LEFT 終點 3 列／INNER 1 列。
  全部節點完成、log 保存 checksum 一致，attempt 在結束後清理。
- 原生 CLI 測試僅將 TableOutput 換成 Dummy；核准資料載入為測試替身，沒有 PostgreSQL 核准驗收，
  沒有 Vertica／模型呼叫。逐筆值仍由上一節原生 compiler probe 證據支援，不以本節筆數取代。

仍保留 `MULTI_SOURCE_EXECUTION_NOT_READY`。接續須把 source-set binding 納入真正的
`execution_authorization`、`execution_reservation`、`RunQueue.begin_external_write` 版本比對，
並接通 Developer schema/prompt、QA execution details、SDM／HWF／manifest／release replay。
這些尚未完成前，不可僅移除授權阻擋就派送真實多來源寫入。

最終隔離 PostgreSQL 回歸：**959 passed、35 skipped、1 warning（20.73s）**，exit 0。
新增兩個 native CLI tests 在一般回歸 opt-in 跳過，但已在上述實際 Worker 另行通過。
線上 API／Worker 未重建，既有 Release 與目標資料未改動。

## Developer V2 交接（模型與正式執行尚未驗收）

- 根據已保存 Run 的來源數選擇 context/proposal V1 或 V2；超過兩來源拒絕。
- V1 prompt version 2、schema 與 context 格式維持原樣；V2 使用 prompt version 3，
  明確傳遞 EtlSpecificationV2、兩來源命名及 Join／CSV evidence。
- V2 必須引用 requirement、join.conditions、sources.csv_inputs；不接受改成 INNER 的 LEFT 規格、
  不匹配版本、浮點／字串版本、缺少引用或上游 checksum 變動。
- API offer、journal 保存、gateway 使用同一個版本選擇器；local bridge 沿用保存的 prompt/schema，
  不重新推測或替換既有呼叫內容。
- 真實隔離 PostgreSQL 整合測試涵蓋 V1/V2 成功保存、過期結果與未知結果；
  驗證單次領取、核准 checksum、保存 prompt/schema 及不可修改的歷史。
  模型回應仍為測試資料，不是 Copilot 真實驗收。

定向測試 **49 passed（0.44s）**；最終隔離回歸
**974 passed、35 skipped、1 warning（21.93s）**，exit 0。
一次本機測試從 repository 根目錄啟動而無法匯入 app；改由 backend 目錄重跑通過，
未修改 import/path 行為來掩蓋問題。

仍保留 `MULTI_SOURCE_EXECUTION_NOT_READY`，未呼叫模型、未寫 Vertica、未重建線上服務。
下一步為 source-set 授權／reservation／write guard、QA context、交付與 replay；
最後再建立新 Join 案例做真實同 Run 驗收，不重跑既有 Release。

## 雙來源綁定與 QA context v4

- 共用 `execution_sources` 驗證來源數、CSV/upload metadata 與每份 SHA-256，完整 source map
  產生固定 checksum；V1 保留原單來源 binding 格式。
- reservation 與 `begin_external_write` 使用共用 `expected_prepared_binding`，
  若授權有 source_checksums，必須完整保留兩份且與 aggregate checksum 一致。
  目前多來源 offer 仍被 `MULTI_SOURCE_EXECUTION_NOT_READY` 阻擋；
  尚未宣稱多來源資料庫 reservation/write 流程已真實驗收。
- QA 重新讀取兩份已驗證上傳的 bytes，各按自己的 CSV 契約檢查；不重跑 ETL、不傳送樣本列。
- 新 QA context v4 綁定 EtlSpecificationV2、明確 Join intent、兩份 CSV 結構證據／checksum、
  compiler plan、output types 與 node references；舊 v1/v2/v3 context 格式不變。
- 交換／缺少某份證據、改變 CSV 契約／Join 類型、不完整掃描或 aggregate checksum 不符皆拒絕。
  QA gateway 已用測試模型回覆驗證 v4 context 傳遞，模型 PASS 無法覆寫 deterministic FAIL。

定向 **46 passed（0.47s）**；隔離 PostgreSQL 全回歸
**993 passed、35 skipped、1 warning（22.07s）**，exit 0。
初始新 fixture 的第二份資料用了逗號、契約卻要求 `|`，被正確拒絕；修正測試資料後通過，
沒有放寬 parser。未呼叫真實模型、未寫 Vertica、未重建線上服務。

下一步：HWF 參數、SDM 的來源／Join 規則、交付模板與 isolated replay 接通；
再移除暫時性 runtime guard，驗證實際多來源授權／reservation／write 拒絕變動及完整同 Run E2E。

## 多來源 HWF、SDM 與可攜重播準備

- HWF 宣告並傳遞 `SOURCE_CSV_0`／`SOURCE_CSV_1`；參數模板說明左右來源，無實際路徑與資料。
  V1 的 SOURCE_CSV 格式保持不變。
- SDM document v2 有來源清單、每欄實際來源歸屬、Join 類型、鍵值、null／重複鍵／字串政策。
  XLSX 對照顯示 source ref，避免兩側同名欄位混淆；規則頁保存全部明確 Join 條件。
- 實際 renderer 產生的 XLSX 通過逐格、封裝與版本比對；修改 Join 後舊 SDM 不可重用。
  ZIP 候選的 HPL／HWF／DDL／PARAMETERS 必須與 compiler bytes 一致，仍不授予 release。
- Replay 在 claim 前核對原 execution authorization 的完整 source set；兩份檔案獨立 staging、
  固定來源參數，執行後逐份校驗。PortabilityEvidenceV2 保存兩份 checksum 與 aggregate，
  正式 Release gate 對照原授權，不接受舊單來源 proof 取代。

驗證：

- 定向 delivery/SDM/proof **29 passed（0.59s）**。
- Replay orchestration **2 passed（0.53s）**：真實檔案暫存，DB／Hop 回應為替身，
  驗證兩來源參數、proof 及來源變動在 claim 前拒絕。這不是 Vertica 重播驗收。
- 禁網原生 Hop2.12.0 HWF **2 passed（9.12s）**：生成 HWF 呼叫生成 HPL，
  僅 TableOutput 換 Dummy；正常雙來源完成、右檔缺失失敗。未使用資料庫或模型。
- 最終隔離 PostgreSQL 回歸 **1002 passed、37 skipped、1 warning（22.17s）**，exit 0。
  新增兩個原生 HWF 測試於回歸中 opt-in 跳過，但已另行真實執行。
- 新 ZIP 測試初次將 manifest 檔名錯寫成 manifest.json；改回既有 release-manifest.json 後通過，
  未改動產品檔名。線上服務未重建，既有 Release 未動。

剩餘接點：`specification_api` 三個入口仍只接受 V1、`specification_editor` 仍單來源預設。
需接通 V2 並驗證 API 保存／核准，再解除 `MULTI_SOURCE_EXECUTION_NOT_READY`，
執行 PostgreSQL 真實授權／reservation／write guard 測試與新案例模型／Hop／Vertica／QA／Release。

## V2 API、授權與執行後 oracle 接通

- 規格 validate／compile-preview／save 支援 V1/V2；editor_context 使用明確雙來源欄位與
  原有已確認 Join intent，不推測 Join。V1 binding 保持不變。
- 隔離 PostgreSQL/API 證明 V2 預覽、保存、核准、歷史讀回；INNER 取代 LEFT 時在保存前
  回報 `join_customers`／`joins.0.join_type`，不保存錯誤規格或生成 HPL。
- 已移除僅代表尚未實作的 `MULTI_SOURCE_EXECUTION_NOT_READY` guard；
  多來源使用 `hop-single-attempt-v3` 並綁定兩份 checksum，V1 仍為 v2。
  仍必須具備有效規格／輸入核准、已核准 oracle、完整上傳、設定／來源指紋、啟用開關、單次租約。
- 真實隔離 PostgreSQL 測試驗證：無 oracle 拒絕、任一來源 checksum 或整組 checksum 改變拒絕、
  缺少 source map 拒絕；只有精確 binding 可 reservation 及標记 write-start，之後不能重複領取或写入。
- 執行後 oracle reader 現在辨識 v3 並再核對完整來源組，避免 Hop 結束後因舊政策判斷卡住。
  測試保存 synthetic log／completion，再讀回同一核准 oracle；不把 synthetic completion 當真實 Hop。

最終隔離回歸：**1004 passed、37 skipped、1 warning（22.58s）**，exit 0。
未呼叫真實模型、未寫 Vertica、未重建 Pilot。此前段落中的 runtime blocker 是歷史狀態，
以本節最新授權實作為準，但仍不代表第 3 階段整體驗收。

下一步：檢視網站規格／SDM 的雙來源與 Join 呈現，重建 Pilot 後使用獨立新 Task，
完成實際模型／Hop／Vertica exact result／QA／isolated portability／Release；保留既有日期 Release。
